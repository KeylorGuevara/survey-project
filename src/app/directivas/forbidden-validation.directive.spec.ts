import { ForbiddenValidationDirective } from './forbidden-validation.directive';

describe('ForbiddenValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbiddenValidationDirective();
    expect(directive).toBeTruthy();
  });
});
